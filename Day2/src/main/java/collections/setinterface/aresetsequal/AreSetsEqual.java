package collections.setinterface.aresetsequal;
import java.util.*;

public class AreSetsEqual {
    public boolean areSetsEqual(HashSet<Integer> set1, HashSet<Integer> set2) {

        // HashSet's equals() method checks for equality.
        return set1.equals(set2);
    }
}
